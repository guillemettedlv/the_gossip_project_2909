<section class="jumbotron text-center bg-white">
<h1 class="jumbotron-heading">Tous les potins de <%= @city.name %></h1>
<% @gossips.each_with_index do |gossip, i| %>
      <p>Message : <%= gossip.title %>
			|  Ecrit part: <%= gossip.user.first_name %>
      |  <%= link_to "Voir le gossip", gossip_path(gossip.id), :class => "btn btn-primary" %>
<% end %>